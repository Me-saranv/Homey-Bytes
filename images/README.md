# Product Images

Product photos uploaded from **Admin → Add Product** are automatically saved here
once GitHub Sync is connected (Admin → Settings → GitHub Sync Setup).

Each upload is committed as its own file, named like:
`<timestamp>-<product-name>.<ext>`

If GitHub Sync isn't connected yet, images are kept only as a local
in-browser preview and won't appear in this folder until you connect it.
